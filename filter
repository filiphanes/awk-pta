#!/bin/bash
# Filters only postings containing tags from args
# Usage: postings | sort | filter 'account ~ /^expenses:/'

gawk "
BEGIN {
    \"date +%Y-%m-%d\" | getline today
}
{
    date = \$1
    amount = \$2
    account = \$3
    payee = \$4
}

$1 {
    print
}"
